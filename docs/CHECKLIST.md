# ✅ Checklist: Проверка всех функций

## 📱 Страница регистрации (index.html)

### Форма
- [ ] Поле "Имя" есть и работает
- [ ] Поле "Телефон" есть и опционально
- [ ] Поле "Событие" загружается из events.json
- [ ] Кнопка отправления работает
- [ ] При незаполненных полях - показывается ошибка

### Камера
- [ ] Кнопка "📸 Включить камеру" запускает видео
- [ ] Кнопка "✓ Снять фото" сохраняет снимок
- [ ] Кнопка "🔄 Переснять" сбрасывает фото
- [ ] После фото показывается preview
- [ ] Камера корректно останавливается

### Успех регистрации
- [ ] После отправления появляется "✓ Успешно!"
- [ ] Показывается уникальный ID
- [ ] Форма сбрасывается через 5 сек
- [ ] Статистика обновляется

### Ошибки
- [ ] Ошибка без имени показывает правильное сообщение
- [ ] Ошибка без события показывает правильное сообщение
- [ ] Ошибка камеры обрабатывается корректно
- [ ] Сообщение об ошибке исчезает через 5 сек

## 🔐 Админ-панель (admin.html)

### Аутентификация
- [ ] Поле ввода пароля присутствует
- [ ] Пароль `admin123` открывает панель
- [ ] Неправильный пароль показывает ошибку
- [ ] Кнопка "Выход" разлогивает пользователя

### Регистрации
- [ ] Таблица загружает сохраненные регистрации
- [ ] Поиск по имени работает
- [ ] Фильтр по событиям работает
- [ ] Кнопка удаления удаляет запись
- [ ] Кнопка "Обновить" перезагружает список

### События
- [ ] Форма добавления события работает
- [ ] События отображаются карточками
- [ ] Кнопка "Удалить" удаляет событие
- [ ] Новые события появляются на главной странице

### Статистика
- [ ] Показывается "Всего регистраций"
- [ ] Показывается разбиение по событиям
- [ ] Дата обновления указывается правильно

### Экспорт
- [ ] Кнопка "📥 Скачать CSV" скачивает файл
- [ ] CSV содержит все регистрации
- [ ] Формат CSV корректный

## 📊 localStorage

После каждой регистрации проверьте:
```javascript
// Консоль браузера (F12)
localStorage.getItem('queue_registrations')  // должен быть JSON с регистрациями
localStorage.getItem('queue_stats')          // должна быть статистика
```

## 🔬 Debug функции

### На главной странице
```javascript
// Консоль (F12)
DEBUG.getState()              // показать состояние
DEBUG.exportData()            // экспортировать всё
DEBUG.clearAll()              // очистить всё (ОСТОРОЖНО!)
```

### На админ-панели
```javascript
DEBUG_ADMIN.getState()        // состояние админки
DEBUG_ADMIN.exportRegistrations()  // все регистрации
```

## 🎯 Сценарий полного тестирования

### 1. Начальное состояние
```bash
# Браузер
http://localhost:8080
# Консоль
DEBUG.getState()
# Проверить: events > 0, registrations = []
```

### 2. Добавить регистрацию
```
1. Заполните форму:
   - Имя: "Иван Петров"
   - Телефон: "+7 (999) 111-11-11"
   - Событие: "webinar_01"
   - Комментарий: (опционально)

2. Нажмите кнопку "✓ Зарегистрироваться"

3. Проверите в консоли:
   localStorage.getItem('queue_registrations')
   // Должна быть 1 запись
```

### 3. Добавить еще одну
```
Повторите шаг 2
Должно быть 2 регистрации
```

### 4. Проверить админку
```
1. Перейти на http://localhost:8080/admin.html
2. Пароль: admin123
3. Вкладка "Регистрации"
   - Должны видеть обе регистрации
4. Вкладка "Статистика"
   - "Всего регистраций": 2
```

### 5. Экспортировать
```
1. Админка → Вкладка "Регистрации"
2. Нажать "📥 Скачать CSV"
3. Должен загрузиться файл registrations_ДАТА.csv
4. При открытии - должны быть 2 строки данных
```

### 6. Добавить событие
```
1. Админка → Вкладка "События"
2. Заполните:
   - Название: "Мой вебинар"
   - Тип: "weekly"
   - Начало: 19:00
   - Конец: 21:00
   - Дни: Пн, Ср, Пт
3. Нажать "Добавить событие"
4. Проверить на главной странице:
   - Новое событие должно появиться в dropdown
```

### 7. Удалить регистрацию
```
1. Админка → Регистрации
2. Нажать "✕" на любой
3. Подтвердить удаление
4. Должна остаться 1 регистрация
5. Статистика обновится
```

## ⚠️ Частые проблемы

### "undefined человек" в статистике
**Решение:**
```javascript
// Проверить localStorage
localStorage.getItem('queue_stats')
// Должен быть: {"totalRegistrations": X, ...}
```

### Камера не работает
**Решение:**
- Проверить что используется HTTPS или localhost
- Разрешить доступ браузеру на camera/microphone
- Firefox: about:permissions → camera

### События не загружаются
**Решение:**
```javascript
// Проверить что events.json существует
fetch('/events.json').then(r => r.json()).then(console.log)
```

### Пароль не работает
**Решение:**
- Пароль должен быть ровно: `admin123`
- Проверить что в admin.js правильный хеш:
  ```javascript
  DEFAULT_PASSWORD_HASH = '240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9'
  ```

## ✅ Финальная проверка

Все ли работает? Отметьте галочки:

**Главная страница:**
- [ ] Форма заполняется и отправляется
- [ ] Регистрация сохраняется
- [ ] Статистика обновляется
- [ ] Камера работает (если разрешить)
- [ ] События загружаются

**Админ-панель:**
- [ ] Вход работает
- [ ] Регистрации видны
- [ ] События видны
- [ ] Статистика считается
- [ ] Экспорт CSV работает

**localStorage:**
- [ ] `queue_registrations` содержит данные после каждой регистрации
- [ ] `queue_stats` обновляется

**Debug функции:**
- [ ] `DEBUG.getState()` показывает правильные данные
- [ ] `DEBUG.exportData()` выводит JSON

---

📝 Раз все прошло - система готова к использованию или развертыванию на GitHub Pages!
