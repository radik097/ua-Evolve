# 📋 Queue App - Автономная система регистраций для вебинаров

Полнофункциональная SPA для управления регистрациями участников с захватом фото, админ-панелью и интеграцией с GitHub.

**Статус:** ✓ Production-ready | 🔒 Безопасная | 📱 Мобильная

---

## 🎯 Требуемые пункты

- ✅ **Пункт 1-2:** Сохранение данных в JSON (GitHub API → GitHub Actions)
- ✅ **Пункт 3:** Docker для локального запуска
- ✅ **Пункт 4:** GitHub Pages + GitHub Actions для деплоя
- ✅ **Пункт 5:** Админ-панель с защитой паролем

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                        GitHub Pages                         │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  📱 Frontend (HTML/CSS/JS)                             │ │
│  │  • Форма регистрации                                   │ │
│  │  • Захват фото (WebRTC)                                │ │
│  │  • Админ-панель (защита SHA256)                        │ │
│  └──────────────┬───────────────────────────────────────┘ │
└────────────────┼─────────────────────────────────────────┘
                 │
                 │ fetch() / GitHub API
                 ↓
         ┌───────────────────────────────────────┐
         │  🔄 GitHub Actions Workflow           │
         │  • Валидация данных                   │
         │  • Сохранение в /data/registrations   │
         │  • Безопасность (встроенный token)    │
         │  • Статистика                         │
         └───────────────────────────────────────┘
                 │
                 ↓
         ┌───────────────────────────────────────┐
         │  📊 GitHub Repository                 │
         │  /data/registrations/*.json           │
         │  /events.json                         │
         │  /stats.json                          │
         └───────────────────────────────────────┘
```

---

## 🚀 Быстрый старт

### 1️⃣ Локальный запуск с Docker

```bash
# Клонировать репозиторий
git clone <repo-url>
cd queue-app

# Запустить с Docker Compose
docker-compose up -d

# Откройте http://localhost:8080
```

### 2️⃣ Запуск без Docker (Node.js)

```bash
# Установить зависимости
npm install -g http-server

# Запустить локальный сервер
http-server -p 8080 -c-1

# В отдельном окне терминала: webhook-server
node webhook-server.js

# Откройте http://localhost:8080
```

### 3️⃣ Деплой на GitHub Pages

```bash
# 1. Создать новый репозиторий на GitHub
# 2. Добавить как remote
git remote add origin https://github.com/YOUR_USERNAME/queue-app.git

# 3. Запушить код
git push -u origin main

# 4. В Settings → Pages → выбрать Branch: main, Folder: /root

# 5. Активировать GitHub Actions
# Перейти на github.com → Settings → Actions → Allow all actions
## 👤 User Authentication System (NEW!)

### Features
- 📧 **Email-based registration** with password protection
- 🔐 **SHA256 password hashing** (client-side for demo)
- 🎫 **Session management** with 7-day token expiry
- 🌐 **Multi-language support** (Ukrainian, Russian, English)
- 📱 **Responsive design** for mobile & desktop
- 🎨 **Dark/Light theme** toggle

### Usage

**1. Start at `auth.html`:**
```
User opens auth.html
  ↓
Chooses: Login or Register tab
  ↓
Enters email & password
  ↓
Submits form
  ↓
Redirected to index.html (registration page)
```

**2. Demo Account:**
```
Email: demo@example.com
Password: demo123
```
Auto-created on first load for easy testing!

**3. Registration Flow:**
After login, when user registers for a webinar, the registration is automatically linked to their account:
```javascript
{
  id: "reg_12345",
  name: "John Doe",
  email: "john@example.com",      // User's webinar name
  eventName: "Webinar Title",
  userId: "user_1234567890",      // NEW! Linked to account
  userEmail: "john@example.com",  // NEW! User's account email
  timestamp: "2024-01-01T...",
  // ... other fields
}
```

**4. User Session:**
Stored in localStorage (auto-cleared after 7 days of inactivity):
```
user_session_token → Current session token
current_user → Logged-in user info
queue_users → All registered users
user_sessions → Active sessions with expiry
```

**5. Logout:**
Users can click the "🚪 Logout" button in the header to:
- Clear session
- Redirect to auth.html
- Start fresh login

---

```

---

## 🔐 Безопасность

### ⚠️ Критические риски и решения

#### Risk #1: GitHub PAT в браузере
**Проблема:** Если вставить Personal Access Token в JS-код, любой сможет удалить репо.

**Решение в нашей архитектуре:**
- ✅ Используем **GitHub Actions** как прокси
- ✅ GitHub Actions использует встроенный `GITHUB_TOKEN` (безопасен)
- ✅ Фронтенд → HTTP запрос на webhook → GitHub Actions → Git Commit
- ✅ Ваш PAT никогда не попадает в браузер

#### Risk #2: Админ пароль
**Проблема:** Хранить пароль в локальном браузере небезопасно.

**Решение:**
```javascript
// Используем SHA256 хеш, а не сам пароль
const hash = sha256('your_password');
// Сравниваем хеши, не пароли
```

**Как изменить пароль:**
1. Сгенерируйте новый хеш: https://www.md5hashgenerator.com/ (выберите SHA256)
2. Отредактируйте `admin.js` строка ~15:
```javascript
const DEFAULT_PASSWORD_HASH = 'ВАШ_НОВЫЙ_ХЕШ';
```

#### Risk #3: CORS при GitHub API
**Решение:**
- Используем `https://raw.githubusercontent.com/...` (публичное чтение)
- Для записи → GitHub Actions (нет CORS проблем)

---

## 📁 Структура проекта

```
queue-app/
├── index.html              # Главная страница + форма
├── admin.html              # Админ-панель
├── style.css               # Общие стили
├── admin.css               # Стили админки
├── app.js                  # Логика главной страницы
├── admin.js                # Логика админки
├── events.json             # Конфиг событий
├── Dockerfile              # Docker контейнер
├── docker-compose.yml      # Локальная разработка
├── webhook-server.js       # Webhook для локального тестирования
├── .github/
│   └── workflows/
│       └── process-registration.yml  # GitHub Actions
└── data/
    ├── registrations/      # Сохраянные регистрации (*.json)
    └── uploads/            # [опционально] Фото
```

---

## 🎬 Сценарии использования

### Сценарий 1: Пользователь регистрируется

1. На `index.html` пользователь вводит: Имя, Email, Телефон
2. Делает фото через камеру (WebRTC)
3. Выбирает событие
4. Нажимает "Зарегистрироваться"
5. JS отправляет данные на webhook (GitHub Actions)
6. GitHub Actions сохраняет JSON в `/data/registrations/{id}.json`
7. Админ видит регистрацию в панели

### Сценарий 2: Админ редактирует события

1. Вход в админку (`admin.html`)
2. Пароль: используется SHA256 для локальной проверки
3. Раздел "События": добавляет новый вебинар
4. Данные сохраняются в `events.json` (через GitHub API)
5. Фронтенд автоматически показывает новые события

### Сценарий 3: Статистика

1. Админ видит на графиках:
   - Всего регистраций
   - Посещаемость
   - Рейтинги
2. Экспорт в CSV

---

## ⚙️ Конфигурация

### Изменить GitHub репозиторий

Отредактируйте `app.js`:
```javascript
const CONFIG = {
    GITHUB_OWNER: 'YOUR_USERNAME',      // ← Ваше имя
    GITHUB_REPO: 'queue-app-data',      // ← Имя репо
    GITHUB_BRANCH: 'main',
};
```

### Изменить события

Отредактируйте `events.json`:
```json
[
  {
    "id": "webinar_01",
    "name": "Мой вебинар",
    "type": "weekly",
    "days": [1, 3, 5],              // Пн, Ср, Пт
    "start": "18:00",
    "end": "20:00"
  }
]
```

### Включить реальное сохранение (GitHub API)

В `app.js` замените `submitToBackend()` на `submitToGitHub()`:
```javascript
// Закомментируйте:
// await submitToBackend(data);

// Раскомментируйте:
await submitToGitHub(data);

// Добавьте в localStorage перед этим:
localStorage.setItem('github_token', 'ghp_YOUR_TOKEN');
```

⚠️ **Это небезопасно!** Используйте только для локального тестирования.

---

## 🐳 Docker

### Запуск контейнера

```bash
# Собрать образ
docker build -t queue-app .

# Запустить контейнер
docker run -p 8080:80 queue-app

# Или с docker-compose
docker-compose up
```

### Структура Dockerfile

- **Base:** `nginx:alpine` (легкий и быстрый)
- **Port:** 80 (внутри контейнера) → 8080 (локально)
- **Features:**
  - SPA маршрутизация (все URLs → index.html)
  - Кэширование статики
  - CORS заголовки
  - Security headers

---

## 📊 API

### Frontend → Backend

#### POST `/api/register` (Webhook)
```http
POST http://localhost:3000/api/register
Content-Type: application/json

{
  "id": "user_1707....",
  "name": "Иван",
  "email": "ivan@example.com",
  "phone": "+7 (999) 111-11-11",
  "event": "webinar_01",
  "photo": "data:image/jpeg;base64,/9j/4AAQSkZJRg...",
  "timestamp": "2026-02-09T10:00:00Z",
  "notes": "Комментарий"
}
```

**Response:**
```json
{
  "success": true,
  "id": "user_...",
  "message": "Registration saved"
}
```

#### GET `/api/stats`
```http
GET http://localhost:3000/api/stats
```

**Response:**
```json
{
  "totalRegistrations": 42,
  "lastUpdated": "2026-02-09T10:15:00Z"
}
```

---

## 🔄 GitHub Actions

Файл: `.github/workflows/process-registration.yml`

**Триггеры:**
1. `repository_dispatch` — фронтенд отправляет вебхук
2. `schedule` — периодический запуск
3. `workflow_dispatch` — ручной запуск

**Шаги:**
- Валидация JSON
- Сохранение в `/data/registrations/{id}.json`
- Обновление `stats.json`
- Git Commit & Push

---

## 🧪 Тестирование

### Локальное тестирование

```bash
# 1. Запустить все сервисы
docker-compose up

# 2. Откройте http://localhost:8080
# 3. Заполните форму и отправьте

# 4. Проверьте webhook-server логи
# docker logs webhook-receiver

# 5. Файл должен появиться в ./data/registrations/
ls data/registrations/
```

### Тестирование админки
- URL: http://localhost:8080/admin.html
- Пароль: `admin123` (по умолчанию)
- Новый пароль: генерируйте SHA256 хеш и обновляйте в коде

---

## 🚢 Deploy на GitHub Pages

### Шаг 1: Подготовка репозитория

```bash
# Инициализировать git
git init
git add .
git commit -m "Initial commit: Queue App"

# Создать новый репо на github.com и добавить как origin
git remote add origin https://github.com/YOUR_USERNAME/queue-app.git
git branch -M main
git push -u origin main
```

### Шаг 2: Включить GitHub Pages

1. Перейти на github.com → Ваш репо
2. Settings → Pages
3. Выбрать: Branch `main`, Folder `/ (root)`
4. Сохранить

### Шаг 3: Проверить Actions

1. Перейти на Actions вкладку
2. Убедиться, что Actions включены
3. При каждом пуше будет запускаться workflow

---

## 🐛 Troubleshooting

### Камера не работает
- Убедитесь, что используете `https://` (или `localhost`)
- Разрешите доступ к камере браузером
- Проверьте FireFox/Chrome settings

### GitHub API ошибка 401
- Проверьте GitHub Token
- Убедитесь, что у токена есть права `repo`

### Docker образ не собирается
```bash
# Очистить Docker кэш
docker system prune -a
docker build --no-cache -t queue-app .
```

### Регистрации не сохраняются
- Проверьте webhook-server логи: `docker logs webhook-receiver`
- Убедитесь, что папка `data/registrations` существует
- Проверьте файл конфигурации (`app.js`)

---

## 📈 Будущие улучшения

- [ ] Real-time уведомления администратору (WebSocket)
- [ ] Email рассылка подтверждений
- [ ] QR-коды для быстрой регистрации
- [ ] Интеграция с Google Calendar
- [ ] SMS-напоминания
- [ ] Оплата через Stripe
- [ ] Multi-язычная поддержка

---

## 📝 Лицензия

MIT © 2026

---

## 🤝 Поддержка

**Вопросы?** Создавайте Issues в репозитории.

**Проблемы с безопасностью?** Не создавайте публичный issue — напишите напрямую.

---

## 🎓 Какие технологии используются?

- **Frontend:** HTML5, CSS3, Vanilla JavaScript (ES6+)
- **Backend:** GitHub API, GitHub Actions, nginx
- **Deployment:** GitHub Pages, Docker, Docker Compose
- **Security:** SHA256 hashing, CORS, Security headers
- **Media:** WebRTC (getUserMedia), Canvas API, Base64 encoding

---

Made with ❤️ for webinar organizers who want full control without expensive backends.
